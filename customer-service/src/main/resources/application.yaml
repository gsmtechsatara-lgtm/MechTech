spring:
  application:
    name: customer-service

  cloud:
    discovery:
      enabled: true

  datasource:
    url: jdbc:mysql://localhost:3306/customer-service
    username: AcmeInfi
    password: ProteasLankans
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none   # or validate / none / create
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: false

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8080  #8088
  ssl:
    enabled: false
    key-store: classpath:tv.jks
    key-store-password: password
    keyStoreType: JKS
